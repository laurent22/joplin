Python NLP Pipeline for Joplin Thesaurus plugin.

# Install

```bash
cd packages/plugins/thesaurus/python
python -m venv .venv
source .venv/bin/activate
pip install -e 
```

For a smoother experience, you might also want to look into Astral's `uv`.