1. File without extension and leading `./`: [file1](./sample-extension-less). Get's imported, but filename is converted to extension, like `<internal_id>.file1`
2. File without extension: [file2](sample-extension-less). Not imported at all.
